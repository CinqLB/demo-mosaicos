'use client'

import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    primary: { main: '#2b6cb0' },
    secondary: { main: '#dd6b20' },
  },
  shape: {
    borderRadius: 8,
  },
})
